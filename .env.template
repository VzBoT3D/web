VZ_TOKEN=

VZ_LOG_CHANNEL=
VZ_ADMIN_ROLE=
VZ_TEAM_ROLE=
VZ_SERIAL_CATEGORY=
VZ_SERIAL_ANNOUNCEMENT_CHANNEL=

VZ_SERIAL_BASE_PLATE_LOCATION=/data/serial-base-plates
VZ_SERIAL_NUMBER_PLATES_LOCATION=/data/serial-number-plates

# Host paths to the STL files (mounted into the bot container)
VZ_SERIAL_BASE_PLATE_HOST_PATH=
VZ_SERIAL_NUMBER_PLATES_HOST_PATH=

VZ_WEBSITE_URL=""

# Monitoring (both optional — leave empty to disable)
# Pinged only while the bot is healthy. If the pings stop, the external service alerts you.
# Works with healthchecks.io, an Uptime Kuma push monitor, BetterStack, etc.
# Give the monitor a grace period of at least 5 minutes.
VZ_HEARTBEAT_URL=
# Discord webhook for outage alerts. Delivered over plain HTTPS, so it still works when the
# bot's gateway connection is the thing that broke.
VZ_ALERT_WEBHOOK_URL=

VZ_DB_USER=vzbot
VZ_DB_ROOT_PASSWORD=vzbot
VZ_DB_HOST=mariadb
VZ_DB_PASSWORD="123"
VZ_DB_DATABASE=vzbot
VZ_DB_PORT=3306

# Frontend
BACKEND_URL=http://bot:8080
BACKEND_TOKEN=
